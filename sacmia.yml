schema_version: 1
standard_version: "1.1.0"
repository_kind: governance
profile: standalone
components:
  github_defaults:
    path: .github
    technology: github
commands:
  lint: git diff --check
  test: python3 scripts/check_markdown_links.py
  build: python3 scripts/check_markdown_links.py
