name: CI

on:
  pull_request:
    branches: [development]
  push:
    branches: [development]

permissions:
  contents: read

jobs:
  ci:
    uses: sacmia/.github/.github/workflows/fastapi-ci.yml@main

